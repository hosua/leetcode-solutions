use std::collections::HashMap;

pub struct Solution;
impl Solution {
    pub fn is_alien_sorted(words: Vec<String>, order: String) -> bool {
        let mp: HashMap<char, i32> = HashMap::new();
        for ch in order.chars().enumerate() {
        }

        false
    }
}
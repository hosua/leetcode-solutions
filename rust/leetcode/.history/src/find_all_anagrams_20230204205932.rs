pub struct Solution;
use std::vec::Vec;

impl Solution {
    pub fn find_anagrams(s: String, p: String) -> Vec<i32> {
        let mut res: Vec<i32>;
        const S: usize = s.len();        
        const P: usize = p.len();

        let s_count: Vec<usize> = vec![];

        for i in P..S {

        }

        res
    }
}
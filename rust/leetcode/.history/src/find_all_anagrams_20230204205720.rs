pub struct Solution;
use std::vec::Vec;

impl Solution {
    pub fn find_anagrams(s: String, p: String) -> Vec<i32> {
        const S: usize = s.len();        
        const P: usize = P.len();
    }
}
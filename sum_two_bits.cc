#include <bits/stdc++.h>

#define NUM_BITS 32
using namespace std;
class Solution {
public:
    int getSum(int a, int b) {
    }
};

int main(){
	Solution sol;
	int a = 1, b = 2;
	cout << sol.getSum(a, b);
}
